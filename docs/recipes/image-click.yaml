version: 1
steps:
  - name: click-buy
    image: templates/buy-button.png
    confidence: 0.9
    region: [0, 400, 1920, 400]   # [left, top, width, height] — restrict search area
    verify_transition: true
    transition_threshold: 0.02
