# CLAUDE

## Code Principles

This repository follows the shared Python engineering guide at
`.claude/python-engineering.md`
for Python code writing, review, and refactoring.

The same guidance is also available as a shared skill at
`.claude/skills/python-engineering/SKILL.md`.

When that guide conflicts with `AGENTS.md`, the PRD set, `pyproject.toml`, or
the repository validation contract, the repository canonical sources win.
